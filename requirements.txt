# ─────────────────────────────────────────────────────────
# Futebol Elite — Dependências
# ─────────────────────────────────────────────────────────

# Motor estatístico
numpy>=1.24
scipy>=1.10

# Dados e HTTP
pandas>=2.0
requests>=2.28

# API REST
fastapi>=0.104
uvicorn[standard]>=0.24
pydantic>=2.0

# Banco de dados
sqlalchemy>=2.0
# Driver PostgreSQL — necessário sempre que DATABASE_URL usar postgresql://,
# que é exatamente o caso do docker-compose.yml. Com ele comentado, todos os
# containers subiam e morriam com ModuleNotFoundError: No module named 'psycopg2'.
psycopg2-binary>=2.9

# Dashboard
# >=1.49 por causa do parâmetro `width` (substituto de use_container_width).
streamlit>=1.49
plotly>=5.18

# Bot/agendador
schedule>=1.2

# Utilitários
python-dotenv>=1.0   # carrega .env (usado em config.py)
